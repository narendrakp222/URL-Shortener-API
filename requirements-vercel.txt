# Runtime dependencies for the Vercel serverless deployment.
#
# Vercel installs this file (preferred over requirements.txt) for the
# @vercel/python builder. Test-only packages are intentionally excluded to keep
# the unzipped function bundle below Vercel's 250 MB limit.
fastapi>=0.110.0,<1.0.0
sqlalchemy[asyncio]>=2.0.28,<3.0.0
asyncpg>=0.29.0,<1.0.0
aiosqlite>=0.20.0,<1.0.0
pydantic>=2.6.4,<3.0.0
pydantic-settings>=2.2.1,<3.0.0
alembic>=1.13.1,<2.0.0
python-dotenv>=1.0.1,<2.0.0
